# Templates

Store templates used for repeatable work (general and role-specific): issue templates, checklists, and reports.

Each template should include:
- Intended use
- Required fields
- Example entry

General templates should be prefixed with `general-`.
